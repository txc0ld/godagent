/**
 * Bidirectional Attention Mock
 * (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class BidirectionalAttention extends BaseMockAttention {
  readonly name = 'bidirectional';
}
