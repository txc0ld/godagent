/**
 * Reformer Attention Mock
 * Locality-sensitive hashing (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class ReformerAttention extends BaseMockAttention {
  readonly name = 'reformer';
}
