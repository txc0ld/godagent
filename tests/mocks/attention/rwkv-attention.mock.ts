/**
 * RWKV Attention Mock
 * Receptance Weighted Key Value (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class RWKVAttention extends BaseMockAttention {
  readonly name = 'rwkv';
}
