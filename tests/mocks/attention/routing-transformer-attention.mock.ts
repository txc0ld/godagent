/**
 * Routing Transformer Attention Mock
 * Content-based routing (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class RoutingTransformerAttention extends BaseMockAttention {
  readonly name = 'routing-transformer';
}
