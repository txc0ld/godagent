/**
 * Luna Attention Mock
 * Linear Unified Nested Attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class LunaAttention extends BaseMockAttention {
  readonly name = 'luna';
}
