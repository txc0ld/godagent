/**
 * Axial Attention Mock
 * Factored attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class AxialAttention extends BaseMockAttention {
  readonly name = 'axial';
}
