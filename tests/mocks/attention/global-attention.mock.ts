/**
 * Global Attention Mock
 * Selected global tokens (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class GlobalAttention extends BaseMockAttention {
  readonly name = 'global';
}
