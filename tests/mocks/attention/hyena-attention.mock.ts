/**
 * Hyena Attention Mock
 * Subquadratic via implicit convolutions (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class HyenaAttention extends BaseMockAttention {
  readonly name = 'hyena';
}
