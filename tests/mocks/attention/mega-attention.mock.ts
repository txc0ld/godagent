/**
 * Mega Attention Mock
 * Moving Average Equipped Gated Attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class MegaAttention extends BaseMockAttention {
  readonly name = 'mega';
}
