/**
 * Differential Attention Mock
 * Differential attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class DifferentialAttention extends BaseMockAttention {
  readonly name = 'differential';
}
