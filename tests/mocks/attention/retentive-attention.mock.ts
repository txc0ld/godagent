/**
 * Retentive Attention Mock
 * Retention-based attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class RetentiveAttention extends BaseMockAttention {
  readonly name = 'retentive';
}
