/**
 * Clustered Attention Mock
 * Cluster-based approximation (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class ClusteredAttention extends BaseMockAttention {
  readonly name = 'clustered';
}
