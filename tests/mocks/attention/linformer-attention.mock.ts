/**
 * Linformer Attention Mock
 * Low-rank self-attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class LinformerAttention extends BaseMockAttention {
  readonly name = 'linformer';
}
