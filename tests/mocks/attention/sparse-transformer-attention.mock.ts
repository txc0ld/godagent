/**
 * Sparse Transformer Attention Mock
 * Fixed sparse patterns (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class SparseTransformerAttention extends BaseMockAttention {
  readonly name = 'sparse-transformer';
}
