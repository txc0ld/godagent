/**
 * FNet Attention Mock
 * Fourier attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class FNetAttention extends BaseMockAttention {
  readonly name = 'fnet';
}
