/**
 * Sliding Window Attention Mock
 * (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class SlidingWindowAttention extends BaseMockAttention {
  readonly name = 'sliding-window';
}
