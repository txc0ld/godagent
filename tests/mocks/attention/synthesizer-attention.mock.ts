/**
 * Synthesizer Attention Mock
 * Random feature attention (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class SynthesizerAttention extends BaseMockAttention {
  readonly name = 'synthesizer';
}
