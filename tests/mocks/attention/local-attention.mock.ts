/**
 * Local Attention Mock
 * Window-based (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class LocalAttention extends BaseMockAttention {
  readonly name = 'local';
}
