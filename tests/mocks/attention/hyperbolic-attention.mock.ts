/**
 * Hyperbolic Attention Mock
 * For hierarchical data (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class HyperbolicAttention extends BaseMockAttention {
  readonly name = 'hyperbolic';
}
