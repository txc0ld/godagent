/**
 * Dilated Attention Mock
 * (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class DilatedAttention extends BaseMockAttention {
  readonly name = 'dilated';
}
