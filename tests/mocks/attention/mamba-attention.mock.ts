/**
 * Mamba Attention Mock
 * State space models (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class MambaAttention extends BaseMockAttention {
  readonly name = 'mamba';
}
