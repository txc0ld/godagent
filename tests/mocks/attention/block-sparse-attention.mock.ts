/**
 * Block Sparse Attention Mock
 * (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class BlockSparseAttention extends BaseMockAttention {
  readonly name = 'block-sparse';
}
