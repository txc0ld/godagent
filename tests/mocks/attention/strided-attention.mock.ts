/**
 * Strided Attention Mock
 * (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class StridedAttention extends BaseMockAttention {
  readonly name = 'strided';
}
