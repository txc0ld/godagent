/**
 * Memory Compressed Attention Mock
 * (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class MemoryCompressedAttention extends BaseMockAttention {
  readonly name = 'memory-compressed';
}
