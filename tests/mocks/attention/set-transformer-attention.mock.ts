/**
 * Set Transformer Attention Mock
 * Attention for sets (MOCK - TEST ONLY)
 */
import { BaseMockAttention } from './base-mock-attention.js';

export class SetTransformerAttention extends BaseMockAttention {
  readonly name = 'set-transformer';
}
